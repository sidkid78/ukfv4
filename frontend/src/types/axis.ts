// ------------------------------------------------------------------------
// 1. Define TypeScript Types (e.g., in src/types/axis.ts)
// ------------------------------------------------------------------------
export interface AxisMetadata {
    index: number;
    key: string;
    name: string;
    description: string;
    formula: string;
    coordinate_rule: string;
  }